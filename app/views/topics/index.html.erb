<div class="container">
  <h1>All Blocmarks</h1>
  <%= link_to "New Topic", new_topic_path, class: "btn btn-primary" %>
  <br>
  <% @topics.each do |topic| %>
    <div class="media">
      <div class="media-body">
        <h4 class="media-heading">
          <%= link_to topic.title, topic %>
          <% if current_user.admin? %>
            <h6><%= button_to "Delete", topic, method: :delete, data: {confirm: 'Are you sure you want to delete this topic?' } %></h6>
          <% end %>
          <div class="row">
            <h6 class="media-heading">
              <% topic.bookmarks.each do |bookmark| %>
                <div class="col-xs-6 col-md-3">
                  <div class="thumbnail">
                    <%= link_to image_tag("#{bookmark.thumbnail_url}"), [bookmark.topic, bookmark] %>
                    <div class="caption">
                      <h3><%= link_to bookmark.url, [bookmark.topic, bookmark] %></h3>
                      <%= render partial: 'likes/like', locals: { bookmark: bookmark } %>
                    </div>
                  </div>
                </div>
              <% end %>
            </h6>
          </div>
        </h4>
      </div>
    </div>
  <% end %>
  <br><br><br>
</div>
